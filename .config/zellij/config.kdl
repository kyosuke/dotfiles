// zellij の設定。デフォルトと異なる項目だけを書く。
// 全オプションの一覧は `zellij setup --dump-config` で確認できる。

// fish を使う。ghostty から GUI 起動されると PATH を解決できず fish を
// 見つけられないため（既定 SHELL の zsh にフォールバックする）絶対パスで指定する。
default_shell "/usr/local/bin/fish"

// macOS のクリップボードへコピー
copy_command "pbcopy"

// 組み込みテーマ
theme "ao"
