# dotfiles

このリポジトリは公開リポジトリ（github.com/kyosuke/dotfiles）で、pushはそのまま公開になる。コミットしてよいのはこのリポジトリ自身の設定とスクリプトだけで、個人情報・機密情報・認証情報は、他のリポジトリやローカルの状態から持ち込んだものも含めて入れない。

`rm -rf` は `node_modules` の掃除を除き、実行前に対象を示して確認を取る。`home/` 配下のファイルは `~` へリンクされているため、消すと稼働中の設定が消える。
